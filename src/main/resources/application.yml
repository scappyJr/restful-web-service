server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages  # 설정 파일명이 "messages", 확장자는 ".properties"

springdoc:
  version: v1.0.0
  api-docs:
    path: /api-docs